import {Container} from '@radix-ui/themes'
import type {ContainerProps} from '@radix-ui/themes/dist/cjs/components/container'

export default (props: ContainerProps) => {
  return <Container {...props} />
}
